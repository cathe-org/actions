name: Deploy Site

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: cathe-org/actions/.github/workflows/deploy-site.yml@main
    with:
      site_directory: /var/www/${{ vars.SITE }}
    secrets: inherit
