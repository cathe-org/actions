name: Deploy Docs

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: cathe-org/actions/.github/workflows/deploy-docs.yml@main
    with:
      subdomain: ${{ github.event.repository.name }}
      source: "./**"
    secrets: inherit